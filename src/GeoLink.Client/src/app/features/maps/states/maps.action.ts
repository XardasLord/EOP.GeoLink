const prefix = '[Maps]';

export class LoadMapBackground {
  static readonly type = `${prefix} ${LoadMapBackground.name}`;
}

export class LoadMapObjects {
  static readonly type = `${prefix} ${LoadMapObjects.name}`;
}
