const prefix = '[Dictionary]';

export class GetSystemGroups {
  static readonly type = `${prefix} ${GetSystemGroups.name}`;
}

export class GetSystemRoles {
  static readonly type = `${prefix} ${GetSystemRoles.name}`;
}

export class GetSystemRegions {
  static readonly type = `${prefix} ${GetSystemRegions.name}`;
}

export class GetSystemPermissions {
  static readonly type = `${prefix} ${GetSystemPermissions.name}`;
}

export class GetMapObjectTypes {
  static readonly type = `${prefix} ${GetMapObjectTypes.name}`;
}

export class GetMapDeviceTypes {
  static readonly type = `${prefix} ${GetMapDeviceTypes.name}`;
}

export class GetMapObjectStatusTypes {
  static readonly type = `${prefix} ${GetMapObjectStatusTypes.name}`;
}

export class GetDeviceGroupsRelation {
  static readonly type = `${prefix} ${GetDeviceGroupsRelation.name}`;
}

export class GetTimeExtentDefinitions {
  static readonly type = `${prefix} ${GetTimeExtentDefinitions.name}`;
}

export class GetConfigDefinitions {
  static readonly type = `${prefix} ${GetConfigDefinitions.name}`;
}

export class GetFilterAttributeDefinitions {
  static readonly type = `${prefix} ${GetFilterAttributeDefinitions.name}`;
}
